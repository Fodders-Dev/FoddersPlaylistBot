# Memebot

Автоматический Telegram-бот, который подбирает мемы из Pinterest-рекомендаций, публикует их в каналах и прокачивает лучшие посты обратно в доску. Проект задуман как многоарендный (каждый канал хранится как конфигурация в БД) и поддерживает расширяемые источники контента (Pinterest, музыкальные плейлисты, кастомные RSS и т. д.).

## Возможности

- Автопостинг изображений в заданные telegram-каналы с подписью и inline-кнопками `🔥 Лайк` / `💩 Говно`.
- Счётчик реакций без ограничения по количеству голосов (один пользователь = один активный голос).
- Автопоиск свежих рекомендаций Pinterest: бот хранит уже обработанные пины и берёт только новые.
- При достижении порога лайков бот автоматически прикрепляет мем в Pinterest-доску (влияет на рекомендации).
- При большом количестве дизлайков пост отправляется в «карантин» (можно использовать саб-канал или тред).
- Поддержка дополнительных интерактивностей: «дай название мему», «выбери тему недели», кастомные кнопки.
- Расширяемая архитектура для других источников (музыкальный плейлист, Reddit, локальное хранилище).

## Структура

```
src/
  memebot/
    main.py              # Точка входа бота
    config.py            # Работа с env и базовыми настройками
    db.py                # Асинхронный слой хранения (SQLite)
    telegram_bot.py      # Маршрутизация апдейтов и обработка коллбеков
    services/
      autoposter.py      # Планировщик выборки и публикации
      voting.py          # Учёт лайков/дизлайков, триггеры прикреплений
    content_sources/
      base.py            # Базовый протокол источника
      pinterest.py       # Подключение к Pinterest API / RSS
      spotify.py         # Заготовка для музыкального канала
    utils/logging.py     # Настройки логирования
scripts/
  manage.py              # CLI для инициализации / наполнения БД
```

## Быстрый старт

1. **Создай .env** (см. `.env.example`). Укажи токен Telegram-бота, Pinterest токен/куки, путь до БД, интервалы.
2. **Установи зависимости**:
   ```bash
   python -m venv .venv && source .venv/bin/activate
   pip install -e .
   ```
3. **Инициализируй БД и добавь канал**:
   ```bash
   python -m scripts.manage init-db
   python -m scripts.manage register-channel \
     --channel-id "@foddersmemes" \
     --source pinterest \
     --board-id "123456" \
     --like-threshold 25
   ```
4. **Запусти бота**:
   ```bash
   python -m memebot.main
   ```

Бот автоматически стартует планировщик, подцепляет Pinterest рекомендации, постит их и следит за голосами. Порог лайков и поведение при дизлайках можно задавать на канал.

## Pinterest trial / GitHub Pages

Чтобы Pinterest выдал trial API-доступ, нужно показать «белую» воронку: лендинг + политика на отдельном домене и описание ручных действий.

1. **Лендинг и политика**  
   Готовые файлы лежат в `docs/index.html`, `docs/privacy.html` и `docs/styles.css`. Запушь репозиторий в GitHub-организацию [Fodders-Dev](https://github.com/Fodders-Dev) и включи GitHub Pages (Settings → Pages → Deploy from branch → `main` / `docs`). Итоговые ссылки:  
   - `https://<org>.github.io/<repo>/` — лендинг  
   - `https://<org>.github.io/<repo>/privacy.html` — политика
2. **Тексты для заявки**  
   - Название приложения: `Memebot Curator`  
   - Company: `Fodders`  
   - Website: GitHub Pages URL  
   - Privacy: `.../privacy.html`  
   - Scopes: `read_users`, `read_boards`, `read_pins`, `write_pins`  
   - Описание флоу: «OAuth, просмотр рекомендаций через GET /v5/search/pins, ручное сохранение пина POST /v5/pins».
3. **OAuth redirect URIs**  
   Добавь `http://localhost:5173/callback` и `http://127.0.0.1:5173/callback` (можно подставить свой порт). Позже добавишь боевой URL с лендинга.
4. **Видеодемо**  
   Запиши 90-секундное видео (Loom/Drive): лендинг → «Connect Pinterest» → окно OAuth → список рекомендаций → ручной `Save to board` → демонстрация пина в доске.
5. **Заявка**  
   На developers.pinterest.com/apps/connect заполни все поля, приложи иконку 512×512 без логотипов Pinterest и отправь. Если отклонят, создай новое приложение и укажи, что теперь есть лендинг+видео.

> Pinterest важно видеть, что вы *не* делаете массовый автопостинг без взаимодействия пользователя. Везде подчёркивай, что сохранение пина происходит только после клика.

## Расширения

- Добавь собственный источник, реализовав `ContentSource`.
- Настрой музыкальный режим через Spotify-плейлист (см. `content_sources/spotify.py`).
- Поддержка монетизации: в CLI модуле есть заготовка тарифов, можно вести биллинг по количеству постов.

## Лицензия

MIT. Используй, кастомизируй и продавай дальше.
